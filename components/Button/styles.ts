import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        borderRadius: 16,
        backgroundColor: "#2196F3", // android default button color
        padding: 8
    },
    title: {
        fontSize: 16,
        textTransform: "uppercase",
        color: "white",
        fontWeight: "500"
    }
})

export default styles;